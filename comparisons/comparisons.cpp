#include "comparisons.hpp"

bool isGreater_ui_i(unsigned int first, int second) {
  return first > second;
}

bool isGreater_ul_l(unsigned long first, long second) {
  return first > second;
}

bool isGreater_us_s(unsigned short first, short second) {
  return first > second;
}

bool isGreater_us_i(unsigned short first, int second) {
  return first > second;
}

bool isGreater_ui_l(unsigned int first, long second) {
  return first > second;
}
